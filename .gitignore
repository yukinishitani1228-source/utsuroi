# 音源のマスターや変換元の素材は公開サイトに含めない。
# 日本語のファイル名は濁点の扱い（NFC/NFD）が環境で揺れて
# パターンが一致しないことがあるため、
# 「原則すべて除外し、配信するファイルだけを許可する」形にしている。

# 無圧縮の音源マスター
*.wav
*.aiff
*.aif

# 変換元として置いてある素材
_src/

# 音声は配信用のものだけ
*.mp3
!toc-bgm.mp3
!flip.mp3

# 画像はASCII名のジャケ写だけ
*.png
*.jpg
*.jpeg
!*-jacket.png

# 形式の比較用に一時生成したもの
_test*
